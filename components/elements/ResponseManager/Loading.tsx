import { PropsWithChildren, ReactNode } from "react";

function ResponseManagerLoading({ children }: PropsWithChildren<ReactNode>) {
  return <>{children}</>;
}

export default ResponseManagerLoading;
